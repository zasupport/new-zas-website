# NEGATIVE control for scripts/check-secrets.sh --test
# The gate's negative control (a planted credential MUST be caught) is generated
# at RUNTIME inside a temp dir by `scripts/check-secrets.sh --test`, never stored
# here — a public repo must not carry a secret-shaped token, even as a fixture,
# or the gitleaks CI leg this gate protects would re-break (§384). This file
# exists only to satisfy the hook-block fixture-presence check (guard matches the
# stem, not the content). See --test for the live positive/absence controls.
