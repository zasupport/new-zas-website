# ABSENCE control for scripts/check-secrets.sh
# When gitleaks is not installed, the gate must FAIL CLOSED (refuse to pass
# unscanned on a public repo), not silently allow. Verified via the --pre-commit
# path with gitleaks absent. Never a silent pass, never a crash.
