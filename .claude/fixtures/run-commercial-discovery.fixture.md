# Fixture / equivalent-control record for scripts/run-commercial-discovery.sh
# Non-lifecycle orchestrator (hook-block-mandate exemption). Control: the discovery-*.py --test suite
# it chains + test-commercial-discovery.py. Advisory/read-only; writes only under ~/.za-commercial-discovery.
